# nixpacks.toml (create in ROOT directory)
[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install] 
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT"
